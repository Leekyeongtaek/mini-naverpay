spring:
  application:
    name: mini-pay-event
  profiles:
    include: secret
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
